# Katalog skillů

> Generováno automaticky: `bash install-skills.sh --gen-readme`. Needituj ručně.

Repo nese katalog; každý server si vybere jen relevantní skilly:

```
bash install-skills.sh                 # vypíše tento katalog
bash install-skills.sh docker-ops      # nainstaluje vyjmenované
bash install-skills.sh --detect        # osahá projekt a nainstaluje pasující
```

| Skill | Kdy použít | Tagy |
|---|---|---|
| `astro-builder` | Staví a upravuje malé weby a blogové podstránky v Astro. | [astro, web, frontend, node] |
| `docker-ops` | Provoz a diagnostika Docker kontejnerů na serveru — stav, logy, restart, compose, čištění místa. | [docker, ops, containers, compose] |
